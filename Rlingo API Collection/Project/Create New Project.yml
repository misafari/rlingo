info:
  name: Create New Project
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/projects"
  body:
    type: json
    data: |-
      {
        "name": "sample project",
        "description": "this is just for test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
